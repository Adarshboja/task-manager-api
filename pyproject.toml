[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "task-manager-api"
version = "1.0.0"
description = "Generated FastAPI service"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
